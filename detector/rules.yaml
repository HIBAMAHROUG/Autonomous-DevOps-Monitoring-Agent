# DEMO VALUES: lower duration/thresholds for validation.
# Restore stricter values after the end-to-end test.
cpu:
  threshold: 70
  duration: 60
  trend_multiplier: null

memory:
  threshold: 70
  duration: 60
  trend_multiplier: null

maintenance_windows: []